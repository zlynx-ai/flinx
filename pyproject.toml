[project]
name = "flinx"
version = "0.1.0"
description = "JAX/Flax model implementation/training library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flax>=0.12.5",
    "jax>=0.9.1",
    "optax>=0.2.6",
    "orbax>=0.1.9",
    "tokenizers>=0.22.2",
]
