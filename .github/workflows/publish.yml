name: Publish to PyPI

on:
    push:
        tags:
            - 'v*'

jobs:
    build-n-publish:
        name: Build and publish to PyPI
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read

        steps:
            - uses: actions/checkout@v4
            
            - name: Install uv
              uses: astral-sh/setup-uv@v5

            - name: Set up Python
              uses: actions/setup-python@v5
              with: 
                python-version-file: ".python-version"

            - name: Build package
              run: uv build

            - name: Publish to PyPI
              uses: pypi/github-action-pypi-publish@release/v1